<div class="well">
  <%= form_tag send("crews_path"), method: :get do |f| %>
    <div class="row">
      <div class="col-md-4">
        <%= text_field_tag :q, @q, class: "form-control", placeholder: "Quick Search by Name" %>
      </div>
      <div class="col-md-3">
        <%= select_tag :vessel_id, options_from_collection_for_select(Vessel.select("*").order(:name), :id, :name, @vessel.try(:id)), class: "form-control select-select2", prompt: "By Vessel" %>
      </div>
      <div class="col-md-3">
        <%= select_tag :rank_id, options_from_collection_for_select(Rank.all, :id, :name, @rank.try(:id)), prompt: "By Rank", class: "form-control select-select2" %>
      </div>
      <div class="col-md-2">
        <%= submit_tag "Search", class: "btn btn-info btn-block" %>
      </div>
    </div>
  <% end %>
</div>

