<div class="for_non_mobile">
  <div id="latest_crew">
    <% if !crew.nil? %>
    <h4>Latest Added Crew <small><%= crew.created_at.strftime('%B %e, %Y') %></small></h4>
      <div class="well">
        <div class="row">
          <div class="col-md-3">
            <%= image_tag crew.picture.url(:standard) %>
          </div>
          <div class="col-md-1">
          </div>
          <div class="col-md-8">
            <h3><%= crew.to_s_list %> <small><%= crew.code_number %></small></h3>
            <h4><%= crew.rank %></h4>
            <div class="">
              <%= link_to edit_crew_path(crew), class: "btn btn-xs btn-default" do %>
                <i class="glyphicon glyphicon-pencil"></i> Edit
              <% end %>
              <%= link_to crew_path(crew), class: "btn btn-xs btn-default" do %>
                <i class="glyphicon glyphicon-user"></i> Profile
              <% end %>
              <%= link_to crews_path, class: "btn btn-xs btn-default" do %>
                <i class="glyphicon glyphicon-folder-close"></i> Go to Database
              <% end %>
            </div>  
          </div>
        </div>
      </div>
    <% else %>
      No crew added. Please <%= link_to "add one first", new_crew_path %>.
    <% end %>
  </div>
</div>

<div class="for_mobile">
  <div id="latest_crew_mobile">
  <% if !crew.nil? %>
  <h4>Latest Added Crew <small><%= crew.created_at.strftime('%B %e, %Y') %></small></h4>
    <div class="well">
      <div class="row">
        <div class="col-md-3">
          <%= image_tag crew.picture.url(:standard) %>
        </div>
        <div class="col-md-1">
        </div>
        <div class="col-md-8">
          <h3><%= crew.to_s_list %> <small><%= crew.code_number %></small></h3>
          <h4><%= crew.rank %></h4>
          <div class="">
            <%= link_to edit_crew_path(crew), class: "btn btn-xs btn-default" do %>
              <i class="glyphicon glyphicon-pencil"></i> Edit
            <% end %>
            <%= link_to crew_path(crew), class: "btn btn-xs btn-default" do %>
              <i class="glyphicon glyphicon-user"></i> Profile
            <% end %>
            <%= link_to crews_path, class: "btn btn-xs btn-default" do %>
              <i class="glyphicon glyphicon-folder-close"></i> Go to Database
            <% end %>
          </div>  
        </div>
      </div>
    </div>
  <% else %>
    No crew added. Please <%= link_to "add one first", new_crew_path %>.
  <% end %>
</div>

</div>
