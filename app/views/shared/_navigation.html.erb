<nav class="navbar-wrapper navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button data-toggle="collapse" class="navbar-toggle collapsed" type="button" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
  </div>
  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav my_nav">
      <li class="<%= params[:controller] == 'pages' && params[:action] ? 'active' : '' %>">
          <%= link_to root_path do %>
            <span class="glyphicon glyphicon-home"></span> DASHBOARD
          <% end %>
      </li>
      <!-- DROPDOWN -->
      <li class="dropdown">
      <%= link_to "#", :class => "dropdown-toggle", "data-toggle" => "dropdown" do %>
          <span class="glyphicon glyphicon-user"></span> CREW LIST <b class="caret"></b>
      <% end %>
        <ul class="dropdown-menu">
          <li>
            <%= link_to crews_path do%>
              <span class="glyphicon glyphicon-chevron-right"></span> Active Crew
            <% end %>
          </li>

           <li>
            <%= link_to crews_advanced_search_path do%>
              <span class="glyphicon glyphicon-chevron-right"></span> Advanced Search
            <% end %>
          </li>
        </ul>
      </li>
      <li class="<%= params[:controller] == 'vessels' ? 'active' : '' %>"> 
         <%= link_to vessels_path do %>
            <span class="icon-flag"></span> VESSELS
          <% end %>
      </li>
      <li class="<%= params[:controller] == 'principals' ? 'active' : '' %>"> 
         <%= link_to principals_path do %>
            <span class="icon-user"></span> PRINCIPAL
          <% end %>
      </li>

       <li class="<%= params[:controller] == 'transmittal_records' ? 'active' : '' %>"> 
         <%= link_to transmittal_records_path do %>
            <span class="glyphicon glyphicon-list-alt"></span> TRANSMITTAL RECORDS
          <% end %>
      </li>

      <li class="dropdown">
      <%= link_to "#", :class => "dropdown-toggle", "data-toggle" => "dropdown" do %>
           <span class="icon-copy"></span> REPORTS <b class="caret"></b>
      <% end %>

        <ul class="dropdown-menu">
          <li>
            <%= link_to reports_crew_manifest_path do%>
              <span class="glyphicon glyphicon-chevron-right"></span> Crew Manifest
            <% end %>
          </li>
        </ul>
      </li>
    </ul> 
    <div class="navbar-form navbar-right">
      <!--<b class="logged-in" style="color:white;">Logged in as:</b> <span class="login-text"><%= current_user.email %></span>-->
      <%= link_to "Logout", destroy_user_session_path, method: :delete, data: { confirm: "Are you sure you want to log-out?" }, class: "btn btn-danger" %>
    </div>
  </div><!-- /.navbar-collapse -->
  </div>
</nav>
